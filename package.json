{
  "name": "carti-retail-shopping",
  "version": "1.0.0",
  "private": true,
  "description": "Retail shopping take-home: NestJS BFF + React Native client",
  "workspaces": [
    "bff",
    "mobile"
  ],
  "scripts": {
    "install:all": "npm install --workspaces --include-workspace-root",
    "bff": "npm run start:dev --workspace=bff",
    "mobile": "npm run start --workspace=mobile",
    "test": "npm run test --workspace=bff && npm run test --workspace=mobile",
    "test:bff": "npm run test --workspace=bff",
    "test:mobile": "npm run test --workspace=mobile"
  }
}
